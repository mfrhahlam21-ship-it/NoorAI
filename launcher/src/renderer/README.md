Renderer (React + Vite) application for the NoorAI Launcher.
This intentionally does NOT modify the web frontend. The launcher loads the web platform URL in its main window and also provides a local UI for system tools.
